/*#include<iostream>
#include<string>
#include<stdlib.h>
using namespace std;

int main() {
	string str;
	char *st;
	
	for (;;) {
		getline(cin, str);
		st = (char *)malloc(sizeof(char)*(str.length()+1));
		strcpy(st, str.c_str());
		cout << st;
		free(st);
	}
	for (;;) {
		for (;;) {
			for (;;) {

			}
		}
	}


}*/
